"""
Read-only FastAPI exposing ftmq statement stores and the `ftmq.search`
full-text index (formerly the standalone `ftmq-api` package). Requires the
`api` extra. Run it with granian: `granian --interface asgi ftmq.api.app:app`.
"""
